macros:
  - name: cents_to_dollar
    description: convert cents into dollars
    arguments:
      - name: column_name
        type: string
        description: name of the column you want to convert
      - name: decimal 
        type: integer
        description: number of decimal place